# The library declares expo/react/react-native as peerDependencies. Don't
# auto-install them locally: their transitive Metro would conflict with the
# sibling example app's Metro. The library builds against expo-modules-core.
legacy-peer-deps=true
