// Entry point for the Expo config plugin. Expo auto-detects `app.plugin.js` at
// the package root and uses its default export. The plugin source lives in
// `plugin/src` and is compiled to `plugin/build` (published; git-ignored).
module.exports = require('./plugin/build');
